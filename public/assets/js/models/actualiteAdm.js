var ModelActualiteAdm = Pclia.Model.extend({
    // validate: function(attrs, options) {
    //     if (!_.isString(attrs.title)) {
    //         return 'le titre doit être une chaîne de caractères';
    //     },
    //     if (!_.isDate(attrs.date)) {
    //         return 'la date doit être une date jj-mm-aaaa';
    //     },
    //     if (!_.isString(attrs.description)) {
    //         return 'la description doit être une chaîne de caractères';
    //     },
    // }
});